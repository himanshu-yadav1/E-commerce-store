import { Router } from "express"

const router = Router()

// router.get('/getCartItems', )
// router.post('/addToCart/:id', )
// router.delete('/removeItemFromCart/:id', )
// router.delete('/clearCart', )
// router.post('/updateQuantity/:id', )

export default router